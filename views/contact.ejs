<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us - Foodify</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        /* Body Style */
        body {
            background-color: #121212;
            color: #e0e0e0;
            line-height: 1.6;
            animation: fadeIn 1s ease-in-out;
        }

        /* Navigation Bar */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background: #1e1e1e;
            opacity: 0;
            animation: slideInNav 1s forwards;
        }

        nav .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: #f8c74c;
        }

        nav ul {
            display: flex;
            list-style: none;
        }

        nav ul li {
            margin-left: 20px;
        }

        nav ul li a {
            color: #e0e0e0;
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.3s;
        }

        nav ul li a:hover {
            color: #f8c74c;
        }

        /* Contact Section */
        .contact-section {
            padding: 50px 20px;
            text-align: center;
            animation: fadeInUp 1s ease-out;
        }

        .contact-section h1 {
            font-size: 2.5rem;
            color: #f8c74c;
            margin-bottom: 20px;
            opacity: 0;
            animation: fadeInUp 1s ease-out 0.5s forwards;
        }

        .contact-section p {
            font-size: 1.2rem;
            color: #cccccc;
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeInUp 1s ease-out 1s forwards;
        }

        .contact-form {
            max-width: 600px;
            margin: 0 auto;
            background: #1e1e1e;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            opacity: 0;
            animation: fadeInUp 1s ease-out 1.5s forwards;
        }

        .contact-form label {
            display: block;
            font-size: 1rem;
            margin-bottom: 5px;
            color: #f8c74c;
        }

        .contact-form input,
        .contact-form textarea,
        .contact-form button {
            width: 100%;
            margin-bottom: 15px;
            padding: 10px;
            font-size: 1rem;
            border-radius: 5px;
            border: none;
        }

        .contact-form input,
        .contact-form textarea {
            background: #2c2c2c;
            color: #e0e0e0;
            opacity: 0;
            animation: fadeInUp 1s ease-out 2s forwards;
        }

        .contact-form button {
            background: #f8c74c;
            color: #121212;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s;
        }

        .contact-form button:hover {
            background: #ffae42;
        }

        #formResponse {
            margin-top: 20px;
            color: #f8c74c;
            font-size: 1.2rem;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background: #121212;
            color: #757575;
            margin-top: 40px;
            opacity: 0;
            animation: fadeInUp 1s ease-out 3s forwards;
        }

        footer p {
            font-size: 0.8rem;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideInNav {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav>
        <div class="logo">Foodify</div>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/menu">Menu</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="/contact">Contact</a></li>
        </ul>
    </nav>

    <!-- Contact Section -->
    <section class="contact-section">
        <h1>Contact Us</h1>
        <p>We’d love to hear from you! Use the form below to get in touch.</p>
        <div class="contact-form">
            <form id="contactForm" action="/create" method="get">
                <label for="name">Your Name</label>
                <input type="text" id="name" name="name" placeholder="Enter your name" required>

                <label for="email">Your Email</label>
                <input type="email" id="email" name="email" placeholder="Enter your email" required>

                <label for="message">Your Message</label>
                <!-- <input id="message" name="message" rows="5" placeholder="Write your message here" required> -->
                <textarea id="message" name="message" rows="5" placeholder="Write your message here" required></textarea>

                <button type="submit">Send Message</button>
            </form>
        </div>
        <div id="formResponse"></div>
    </section>

    <!-- Footer -->
    <footer>
        <div style="padding: 20px; background: #1e1e1e; color: #e0e0e0; text-align: center;">
            <div style="margin-bottom: 20px;">
                <h2 style="color: #f8c74c; font-size: 1.5rem;">Connect With Us</h2>
                <div>
                    <a href="https://facebook.com" target="_blank" style="margin: 0 10px; color: #f8c74c; text-decoration: none; font-size: 1.2rem;">Facebook</a> |
                    <a href="https://twitter.com" target="_blank" style="margin: 0 10px; color: #f8c74c; text-decoration: none; font-size: 1.2rem;">Twitter</a> |
                    <a href="https://instagram.com" target="_blank" style="margin: 0 10px; color: #f8c74c; text-decoration: none; font-size: 1.2rem;">Instagram</a>
                </div>
            </div>        
            <div>
                <p style="font-size: 0.9rem; margin-bottom: 5px;">123 Foodify Lane, Delicious City, FL 45678</p>
                <p style="font-size: 0.9rem; margin-bottom: 5px;">Email: <a href="mailto:support@foodify.com" style="color: #f8c74c; text-decoration: none;">support@foodify.com</a></p>
                <p style="font-size: 0.9rem;">Phone: (123) 456-7890</p>
            </div>
        
            <div style="margin-top: 20px; font-size: 0.8rem; color: #757575;">
                <p>© 2024 Foodify. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- JavaScript -->
    <script>
        document.getElementById('contactForm').addEventListener('submit', function (e) {
            e.preventDefault(); // Prevent default form submission

            const formData = new FormData(this);
            const responseDiv = document.getElementById('formResponse');

            // Simulate server response
            setTimeout(() => {
                const success = Math.random() > 0.5; // Random success or failure
                if (success) {
                    responseDiv.textContent = "Thank you! Your message has been sent.";
                    document.getElementById('contactForm').reset();
                } else {
                    responseDiv.textContent = "Thank you! Your message has been sent.";
                }
            }, 1000);
        });
    </script>
</body>
</html>
